import React from 'react';
import cn from 'classnames';

import * as styles from './{{name}}.module.css';

const {{name}} = ({ props }) => {
  return (
     <div></div>
  );
};

export default {{name}};
